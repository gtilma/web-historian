// Use the code in `archive-helpers.js` to actually download the urls
// that are waiting.
var helpers = require('../helpers/archive-helpers');

// readListOfUrls
// for each url in list (sites.txt)
  // isUrlArchived ?
    // No ? push on to 'pending' array

// return 'pending' array